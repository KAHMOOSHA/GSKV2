import { Merriweather_Sans, Nunito_Sans, Playwrite_AU_QLD } from 'next/font/google';

export const merriweather = Merriweather_Sans({
  subsets: ['latin'],
});

export const nunito = Nunito_Sans({
  subsets: ['latin'],
});

export const playwrite = Playwrite_AU_QLD({
  weight: ['100', '200', '300', '400'],
});
